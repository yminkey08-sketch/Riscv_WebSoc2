# 日报 2026-08-05

## 任务一：Ping 调通

### 进展和结果
- 同步 RiscV_webSoC2(2).7z 源码到工作目录，Ping 169.254.1.1 通过 (0.8ms, 0% loss)
- 修复 linker.ld 加 `/DISCARD/` 解决 GCC12 的 .note.gnu.build-id 占位 0x00 问题

### 遇到的问题
- updatemem 后固件不运行：原因是 bank 文件从 c_build 拷贝到 build_xilinx 时用了旧文件
- base bitstream 必须用 v3 项目的 RiscV_WebSoC_v3base.bit，impl_1 目录下的不可用

## 任务二：TCP 协议栈实现

### 进展和结果
- 完成发送发动机（fill_tcp_header + tcp_checksum + send_tcp_seg）
- 完成接收解析器（从 RX FIFO 读 TCP 头，解析 flags/seq/ack/port）
- 完成连接表管理（并行数组，MAX_CONNECTIONS=4）+ 状态机（CLOSED→SYN_RECEIVED→ESTABLISHED→FIN_WAIT→TIME_WAIT）
- 集成到 program_main，编译通过，Ping 仍正常

### 遇到的问题
- nc -vz 169.254.1.1 80 超时，TCP 三次握手未通过
- 上板硬调效率太低，已加 LED 断点辅助定位

## 明天计划
- 定位 TCP 握手失败根因并修复
