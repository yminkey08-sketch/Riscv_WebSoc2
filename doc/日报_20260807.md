# 日报 2026-08-07

## TCP 协议栈

### 进展和结果
- 定位 TCP 三次握手失败根因：`fill_tcp_header` 端口没交换，PC 收到 SYN+ACK 无法匹配
- 修复后 `nc -vz 169.254.1.1 80` 通过
- 实现 TCP 短连接 LED 控制：发 1 字节 payload 控制 4 路 LED

### 遇到的问题
- `LCPU_SET_LED` 在 FIFO 操作期间调用会破坏网络通信——改为在 RX/TX 操作之外设置
- TCP 连接快速建立后不释放——加了 `tcp_periodic_check()` 周期性清理
- curl 走代理 502 错误——`--noproxy '*'` 直连解决

## Verilator 仿真

### 进展和结果
- 搭建 Verilator 仿真环境，替代 iverilog（编译式仿真，速度快）
- 固件通过 `$readmemh` 预加载到 BRAM，绕过 BFM
- 补全了 altsyncram/xpm_memory_tdpram stub 的缺失端口和参数
- Verilog 编译通过，C++ testbench 框架就绪

### 遇到的问题
- XPM/altsyncram stub 需大量补充端口才能匹配 Xilinx 原语——逐一补齐
- GMII 直连接口已就绪，待上板交叉验证

### 明天计划
- Verilator 仿真跑通，注入 ARP/TCP 包观察 TX 响应
