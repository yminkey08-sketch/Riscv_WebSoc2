# 日报 2026-08-07

## TCP 收尾 + Verilator 仿真

### 进展和结果
- TCP 三次握手 `nc -vz` 验证通过，确认昨天端口交换修复正确
- 完成 TCP 短连接 LED 控制：单字节 payload 控制 4 路 LED，Python 客户端 6/6 通过
- 搭建 Verilator 编译式仿真环境，替代 iverilog
- 补全 altsyncram / xpm_memory_tdpram stub 缺失端口和参数，Verilog 编译通过
- 固件改为 `$readmemh` 预加载到 BRAM，C++ testbench 直接注入 GMII 包

### 遇到的问题
- Verilator 比 iverilog 严格：XPM/altsyncram stub 需匹配完整 Xilinx 原语端口——逐一补了 `clocken2/3`、`byteena_a/b`、`rden_a/b`、`eccstatus` 等十几个缺失端口

## ILA 调试

### 进展和结果
- 添加 ILA IP 核，抓取 GMII RX/TX 和 cpu_rd_empty 信号
- 上板触发抓包，观察 ARP/ICMP 回包的 GMII 波形，验证 MAC 层收发正确

### 明天计划
- 完成 HTTP 网页显示和控制功能，上板验证
